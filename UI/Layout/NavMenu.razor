<div class="brand">
    <div class="brand-mark">
        <Icon Name="zap" Size="20" />
    </div>
    <div class="brand-text">
        <strong>ElecTrace</strong>
        <span>إدارة فواتير الكهرباء</span>
    </div>
</div>

<nav class="nav-scrollable" @onclick="OnNavigate">
    <p class="nav-section">المتابعة</p>
    <NavItem Href="" Match="NavLinkMatch.All" Icon="home" Text="الرئيسية" />
    <NavItem Href="readings" Icon="zap" Text="قراءات الكهرباء" />
    <NavItem Href="reports" Icon="file-text" Text="التقارير" />

    <p class="nav-section">الإدارة</p>
    <NavItem Href="departments" Icon="building" Text="الأقسام والعدادات" />
    <NavItem Href="users" Icon="users" Text="المستخدمون" />
    <NavItem Href="roles" Icon="shield" Text="الأدوار" />
    <NavItem Href="settings" Icon="settings" Text="الإعدادات" />
</nav>

@code {
    [Parameter] public EventCallback OnNavigate { get; set; }
}
